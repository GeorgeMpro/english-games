describe('Integration Flow: handles backend fetch and image fallback logic', () => {

  it('should use wiki service as fallback for images', () => {
  //   todo
  //   setup needd word items
  //   need to pass them
  });

  xit('should display loader while waiting for images to load', () => {
  });

  xit('should display error message if fallback (wiki service) also fails');
  xit('should ', () => {
  });
});
